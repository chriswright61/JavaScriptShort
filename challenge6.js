// Create a function that takes two strings as a parameters
// and returns the strings concatenated.

// function concatString(string1, string2) {
//   return `${string1} ${string2}`;
// }
// console.log(concatString("Hi", "There"));

concatString = (string1, string2) => {
  return `${string1} ${string2}`;
};
console.log(concatString("Hi", "There"));
